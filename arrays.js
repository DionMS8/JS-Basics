//----------------------JS ARRAYS---------------------------------

//CHECKING IF SOMETHING IS AN ARRAY
console.log(Array.isArray(arrayName))
// THIS WILL RETURN A TRUE OR FALSE IN THE CONSOLE

// CONVERTING SOMETHING INTO AN ARRAY 
Array.from(arrayName)

// ITERATING THROUGH AN ARRAY WITHOUT A EXECUTING A LOOP
arrayName.forEach(element => {
    console.log(element);
});


